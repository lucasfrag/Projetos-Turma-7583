export default class Veiculo {
    constructor(id, modelo, marca, ano, imagem) {
        this.id = id;
        this.modelo = modelo;
        this.marca = marca;
        this.ano = ano;
        this.imagem = imagem;
    }
}